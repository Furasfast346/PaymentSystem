services:
  backend:
    build: ./PaymentSystem
    container_name: Payment_System
    command: sh -c "gunicorn config.wsgi:application --bind 0.0.0.0:8000"
    env_file:
      - PaymentSystem/.env
    ports:
      - "8000:8000"
